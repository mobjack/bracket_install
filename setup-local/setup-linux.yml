---
  - name: "Setup Brackets"
    hosts: localhost
    connection: local
    tasks:

    - name: "Install Packages"
      apt:
        name: [ 'ansible'
                ]
        state: present
        update_cache: yes

